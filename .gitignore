gitignore
# Dependency directories
# Node.js projects universally ignore `node_modules`.
**/node_modules/

# Environment files
# Never commit files with sensitive API keys or credentials.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# visafrontend (React) build output
# Assuming your React app is in a `visafrontend` or `client` folder and builds to a `build` directory.
/visafrontend/build/
/visafrontend/client/build/

# visabackend (Node.js) build output
# Assuming your Node.js visabackend builds to a `dist` or similar directory.
/visabackend/dist/
/visabackend/build/


# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
logs/

# IDE and OS-specific files
# These files are generated by your development environment and should not be shared.
.vscode/
.idea/
.DS_Store
Thumbs.db

# Other common ignore patterns
# Cache files from linters, bundlers, and testing frameworks.
.cache/
.eslintcache
coverage/

# For TypeScript projects
# The generated JavaScript build artifacts are ignored.
*.tsbuildinfo